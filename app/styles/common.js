'use strict';
/**
 * @class
 * @desc login register
 * */
import {
  StyleSheet,
  PixelRatio,
  Dimensions,
} from 'react-native';
var cell_w = Dimensions.get('window').width;
var styles = StyleSheet.create({
    container: {
        backgroundColor:'#fff',
        flex:1,
        alignItems:'center'
    }
})

module.exports = styles;
